<ui:UiPage x:Class="Bloxstrap.UI.Elements.Settings.Pages.QuickLaunchPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      mc:Ignorable="d"
      d:DesignHeight="600" d:DesignWidth="800"
      Title="QuickLaunchPage"
      Loaded="Page_Loaded">
    <StackPanel Margin="0,0,14,14">
        <TextBlock Text="Quick Launch" FontSize="24" FontWeight="Medium" Margin="0,0,0,4" Foreground="#FF69B4" />
        <TextBlock Text="Pin your favorite games for quick access from the launch menu." FontSize="14" Foreground="{DynamicResource TextFillColorSecondaryBrush}" TextWrapping="Wrap" Margin="0,0,0,16" />

        <ui:Card Margin="0,0,0,8">
            <StackPanel>
                <TextBlock FontSize="16" FontWeight="Medium" Text="Add Game" Margin="0,0,0,4" />
                <TextBlock FontSize="12" Foreground="{DynamicResource TextFillColorTertiaryBrush}" Text="Enter a Roblox Place ID to pin" Margin="0,0,0,8" />
                <StackPanel Orientation="Horizontal">
                    <ui:TextBox x:Name="PlaceIdInput" Width="200" PlaceholderText="Place ID (e.g. 2753915549)" Margin="0,0,8,0" />
                    <ui:TextBox x:Name="GameNameInput" Width="200" PlaceholderText="Game Name (optional)" Margin="0,0,8,0" />
                    <ui:Button Content="Add" Icon="Add24" Click="AddGame_Click" />
                </StackPanel>
            </StackPanel>
        </ui:Card>

        <TextBlock Text="Pinned Games" FontSize="16" FontWeight="Medium" Margin="0,8,0,8" />

        <ListView x:Name="PinnedGamesList" MaxHeight="400">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid Margin="0,4">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0">
                            <TextBlock Text="{Binding GameName}" FontWeight="Medium" />
                            <TextBlock Text="{Binding PlaceId}" FontSize="12" Foreground="{DynamicResource TextFillColorTertiaryBrush}" />
                        </StackPanel>
                        <ui:Button Grid.Column="1" Content="Remove" Icon="Delete28" Appearance="Danger" Click="RemoveGame_Click" Tag="{Binding PlaceId}" Padding="8,4" />
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <TextBlock x:Name="QuickLaunchStatus" Margin="0,8,0,0" Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
    </StackPanel>
</ui:UiPage>
