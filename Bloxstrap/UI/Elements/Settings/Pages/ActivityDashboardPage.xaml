<ui:UiPage x:Class="Bloxstrap.UI.Elements.Settings.Pages.ActivityDashboardPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      mc:Ignorable="d"
      d:DesignHeight="600" d:DesignWidth="800"
      Title="ActivityDashboardPage"
      Loaded="Page_Loaded">
    <StackPanel Margin="0,0,14,14">
        <TextBlock Text="Activity Dashboard" FontSize="24" FontWeight="Medium" Margin="0,0,0,4" Foreground="#FF69B4" />
        <TextBlock Text="Track your Roblox play time and activity." FontSize="14" Foreground="{DynamicResource TextFillColorSecondaryBrush}" TextWrapping="Wrap" Margin="0,0,0,16" />

        <Grid Margin="0,0,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <ui:Card Grid.Column="0" Margin="0,0,8,0">
                <StackPanel HorizontalAlignment="Center">
                    <TextBlock x:Name="TotalSessions" Text="0" FontSize="32" FontWeight="Bold" Foreground="#FF69B4" HorizontalAlignment="Center" />
                    <TextBlock Text="Total Sessions" FontSize="12" Foreground="{DynamicResource TextFillColorSecondaryBrush}" HorizontalAlignment="Center" />
                </StackPanel>
            </ui:Card>
            <ui:Card Grid.Column="1" Margin="0,0,8,0">
                <StackPanel HorizontalAlignment="Center">
                    <TextBlock x:Name="TotalPlayTime" Text="0h" FontSize="32" FontWeight="Bold" Foreground="#FF69B4" HorizontalAlignment="Center" />
                    <TextBlock Text="Total Play Time" FontSize="12" Foreground="{DynamicResource TextFillColorSecondaryBrush}" HorizontalAlignment="Center" />
                </StackPanel>
            </ui:Card>
            <ui:Card Grid.Column="2">
                <StackPanel HorizontalAlignment="Center">
                    <TextBlock x:Name="UniqueGames" Text="0" FontSize="32" FontWeight="Bold" Foreground="#FF69B4" HorizontalAlignment="Center" />
                    <TextBlock Text="Unique Games" FontSize="12" Foreground="{DynamicResource TextFillColorSecondaryBrush}" HorizontalAlignment="Center" />
                </StackPanel>
            </ui:Card>
        </Grid>

        <TextBlock Text="Recent Activity" FontSize="16" FontWeight="Medium" Margin="0,0,0,8" />
        <ListView x:Name="ActivityList" MaxHeight="300">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid Margin="0,4">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="{Binding Description}" />
                        <TextBlock Grid.Column="1" Text="{Binding TimeAgo}" Foreground="{DynamicResource TextFillColorTertiaryBrush}" FontSize="12" />
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <TextBlock x:Name="ActivityStatus" Margin="0,8,0,0" Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
    </StackPanel>
</ui:UiPage>
