<ui:UiPage x:Class="Bloxstrap.UI.Elements.Settings.Pages.ThemeEditorPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      mc:Ignorable="d"
      d:DesignHeight="600" d:DesignWidth="800"
      Title="ThemeEditorPage">
    <StackPanel Margin="0,0,14,14">
        <TextBlock Text="Theme Editor" FontSize="24" FontWeight="Medium" Margin="0,0,0,4" Foreground="#FF69B4" />
        <TextBlock Text="Import, export, and customize bootstrapper themes." FontSize="14" Foreground="{DynamicResource TextFillColorSecondaryBrush}" TextWrapping="Wrap" Margin="0,0,0,16" />

        <ui:Card Margin="0,0,0,8">
            <StackPanel>
                <TextBlock FontSize="16" FontWeight="Medium" Text="Current Theme" Margin="0,0,0,4" />
                <TextBlock x:Name="CurrentTheme" FontSize="14" Foreground="{DynamicResource TextFillColorSecondaryBrush}" Text="Unicorn Pink" />
            </StackPanel>
        </ui:Card>

        <ui:Card Margin="0,0,0,8">
            <StackPanel>
                <TextBlock FontSize="16" FontWeight="Medium" Text="Preset Themes" Margin="0,0,0,8" />
                <WrapPanel>
                    <ui:Button Content="Unicorn Pink" Tag="unicorn_pink" Click="PresetTheme_Click" Margin="0,0,8,8" />
                    <ui:Button Content="Ocean Blue" Tag="ocean_blue" Click="PresetTheme_Click" Margin="0,0,8,8" />
                    <ui:Button Content="Forest Green" Tag="forest_green" Click="PresetTheme_Click" Margin="0,0,8,8" />
                    <ui:Button Content="Sunset Orange" Tag="sunset_orange" Click="PresetTheme_Click" Margin="0,0,8,8" />
                    <ui:Button Content="Midnight Purple" Tag="midnight_purple" Click="PresetTheme_Click" Margin="0,0,8,8" />
                    <ui:Button Content="Clean White" Tag="clean_white" Click="PresetTheme_Click" Margin="0,0,8,8" />
                </WrapPanel>
            </StackPanel>
        </ui:Card>

        <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
            <ui:Button Content="Export Theme" Icon="ArrowExportUp24" Click="ExportTheme_Click" Margin="0,0,8,0" />
            <ui:Button Content="Import Theme" Icon="ArrowImport24" Click="ImportTheme_Click" />
        </StackPanel>

        <TextBlock x:Name="ThemeStatus" Margin="0,8,0,0" FontWeight="SemiBold" Visibility="Collapsed" />
    </StackPanel>
</ui:UiPage>
