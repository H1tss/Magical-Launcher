<ui:UiPage x:Class="Bloxstrap.UI.Elements.Settings.Pages.AutoClosePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      mc:Ignorable="d"
      d:DesignHeight="600" d:DesignWidth="800"
      Title="AutoClosePage"
      Loaded="Page_Loaded">
    <StackPanel Margin="0,0,14,14">
        <TextBlock Text="Auto Close" FontSize="24" FontWeight="Medium" Margin="0,0,0,4" Foreground="#FF69B4" />
        <TextBlock Text="Automatically close Magical Launcher when Roblox exits." FontSize="14" Foreground="{DynamicResource TextFillColorSecondaryBrush}" TextWrapping="Wrap" Margin="0,0,0,16" />

        <ui:Card Margin="0,0,0,8">
            <StackPanel>
                <TextBlock FontSize="16" FontWeight="Medium" Text="Enable Auto Close" Margin="0,0,0,4" />
                <TextBlock FontSize="12" Foreground="{DynamicResource TextFillColorTertiaryBrush}" Text="When enabled, Magical Launcher will close itself after Roblox shuts down." Margin="0,0,0,8" />
                <ui:ToggleSwitch x:Name="AutoCloseToggle" IsChecked="False" />
            </StackPanel>
        </ui:Card>

        <ui:Card Margin="0,0,0,8">
            <StackPanel>
                <TextBlock FontSize="16" FontWeight="Medium" Text="Delay (seconds)" Margin="0,0,0,4" />
                <TextBlock FontSize="12" Foreground="{DynamicResource TextFillColorTertiaryBrush}" Text="Wait this many seconds after Roblox closes before shutting down" Margin="0,0,0,8" />
                <StackPanel Orientation="Horizontal">
                    <ui:Button Content="0" Tag="0" Click="DelayPreset_Click" Margin="0,0,8,0" Padding="12,4" />
                    <ui:Button Content="3" Tag="3" Click="DelayPreset_Click" Margin="0,0,8,0" Padding="12,4" />
                    <ui:Button Content="5" Tag="5" Click="DelayPreset_Click" Margin="0,0,8,0" Padding="12,4" />
                    <ui:Button Content="10" Tag="10" Click="DelayPreset_Click" Margin="0,0,8,0" Padding="12,4" />
                </StackPanel>
                <ui:TextBox x:Name="DelayInput" Width="100" PlaceholderText="Seconds" Margin="0,0,8,0" Text="3" />
            </StackPanel>
        </ui:Card>

        <ui:Button Content="Save" Icon="Save24" Margin="0,8,0,0" Click="Save_Click" />
        <TextBlock x:Name="AutoCloseStatus" Margin="0,8,0,0" FontWeight="SemiBold" Visibility="Collapsed" />
    </StackPanel>
</ui:UiPage>
