<ui:UiPage x:Class="Bloxstrap.UI.Elements.Settings.Pages.DiscordRpcPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      mc:Ignorable="d"
      d:DesignHeight="600" d:DesignWidth="800"
      Title="DiscordRpcPage"
      Loaded="Page_Loaded">
    <StackPanel Margin="0,0,14,14">
        <TextBlock Text="Discord Rich Presence" FontSize="24" FontWeight="Medium" Margin="0,0,0,4" Foreground="#FF69B4" />
        <TextBlock Text="Customize your Discord Rich Presence status when playing Roblox." FontSize="14" Foreground="{DynamicResource TextFillColorSecondaryBrush}" TextWrapping="Wrap" Margin="0,0,0,16" />

        <ui:Card Margin="0,0,0,8">
            <StackPanel>
                <TextBlock FontSize="16" FontWeight="Medium" Text="Enable Discord RPC" Margin="0,0,0,4" />
                <TextBlock FontSize="12" Foreground="{DynamicResource TextFillColorTertiaryBrush}" Text="Show your Roblox activity on Discord" Margin="0,0,0,8" />
                <ui:ToggleSwitch x:Name="EnableRpcToggle" IsChecked="True" />
            </StackPanel>
        </ui:Card>

        <ui:Card Margin="0,0,0,8">
            <StackPanel>
                <TextBlock FontSize="16" FontWeight="Medium" Text="Custom Details" Margin="0,0,0,4" />
                <TextBlock FontSize="12" Foreground="{DynamicResource TextFillColorTertiaryBrush}" Text="Override the default detail text" Margin="0,0,0,8" />
                <ui:TextBox x:Name="DetailsInput" PlaceholderText="Playing Roblox" Width="400" />
            </StackPanel>
        </ui:Card>

        <ui:Card Margin="0,0,0,8">
            <StackPanel>
                <TextBlock FontSize="16" FontWeight="Medium" Text="Custom State" Margin="0,0,0,4" />
                <TextBlock FontSize="12" Foreground="{DynamicResource TextFillColorTertiaryBrush}" Text="Override the default state text" Margin="0,0,0,8" />
                <ui:TextBox x:Name="StateInput" PlaceholderText="In Game" Width="400" />
            </StackPanel>
        </ui:Card>

        <ui:Card Margin="0,0,0,8">
            <StackPanel>
                <TextBlock FontSize="16" FontWeight="Medium" Text="Large Image Text" Margin="0,0,0,4" />
                <TextBlock FontSize="12" Foreground="{DynamicResource TextFillColorTertiaryBrush}" Text="Tooltip for the large image" Margin="0,0,0,8" />
                <ui:TextBox x:Name="LargeImageInput" PlaceholderText="Magical Launcher" Width="400" />
            </StackPanel>
        </ui:Card>

        <ui:Card Margin="0,0,0,8">
            <StackPanel>
                <TextBlock FontSize="16" FontWeight="Medium" Text="Small Image Text" Margin="0,0,0,4" />
                <TextBlock FontSize="12" Foreground="{DynamicResource TextFillColorTertiaryBrush}" Text="Tooltip for the small image" Margin="0,0,0,8" />
                <ui:TextBox x:Name="SmallImageInput" PlaceholderText="v1.0.0" Width="400" />
            </StackPanel>
        </ui:Card>

        <ui:Button Content="Save" Icon="Save24" Margin="0,8,0,0" Click="Save_Click" />
        <TextBlock x:Name="RpcStatus" Margin="0,8,0,0" FontWeight="SemiBold" Visibility="Collapsed" />
    </StackPanel>
</ui:UiPage>
