<ui:UiPage x:Class="Bloxstrap.UI.Elements.Settings.Pages.KeyboardShortcutsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      mc:Ignorable="d"
      d:DesignHeight="600" d:DesignWidth="800"
      Title="KeyboardShortcutsPage"
      Loaded="Page_Loaded">
    <StackPanel Margin="0,0,14,14">
        <TextBlock Text="Keyboard Shortcuts" FontSize="24" FontWeight="Medium" Margin="0,0,0,4" Foreground="#FF69B4" />
        <TextBlock Text="Configure global hotkeys for quick actions." FontSize="14" Foreground="{DynamicResource TextFillColorSecondaryBrush}" TextWrapping="Wrap" Margin="0,0,0,16" />

        <ui:Card Margin="0,0,0,8">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="200" />
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="Launch Roblox" VerticalAlignment="Center" />
                <ui:TextBox x:Name="LaunchKey" Grid.Column="1" Width="150" PlaceholderText="Ctrl+Shift+R" />
            </Grid>
        </ui:Card>

        <ui:Card Margin="0,0,0,8">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="200" />
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="Open Settings" VerticalAlignment="Center" />
                <ui:TextBox x:Name="SettingsKey" Grid.Column="1" Width="150" PlaceholderText="Ctrl+Shift+S" />
            </Grid>
        </ui:Card>

        <ui:Card Margin="0,0,0,8">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="200" />
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="Kill Roblox" VerticalAlignment="Center" />
                <ui:TextBox x:Name="KillKey" Grid.Column="1" Width="150" PlaceholderText="Ctrl+Shift+K" />
            </Grid>
        </ui:Card>

        <ui:Card Margin="0,0,0,8">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="200" />
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="Toggle FPS Display" VerticalAlignment="Center" />
                <ui:TextBox x:Name="FpsKey" Grid.Column="1" Width="150" PlaceholderText="Ctrl+Shift+F" />
            </Grid>
        </ui:Card>

        <ui:Button Content="Save Shortcuts" Icon="Save24" Margin="0,8,0,0" Click="Save_Click" />
        <TextBlock x:Name="ShortcutStatus" Margin="0,8,0,0" FontWeight="SemiBold" Visibility="Collapsed" />
    </StackPanel>
</ui:UiPage>
