<ui:UiPage x:Class="Bloxstrap.UI.Elements.Settings.Pages.MultiInstancePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      mc:Ignorable="d"
      d:DesignHeight="600" d:DesignWidth="800"
      Title="MultiInstancePage">
    <StackPanel Margin="0,0,14,14">
        <TextBlock Text="Multi-Instance" FontSize="24" FontWeight="Medium" Margin="0,0,0,4" Foreground="#FF69B4" />
        <TextBlock Text="Run multiple Roblox instances at the same time." FontSize="14" Foreground="{DynamicResource TextFillColorSecondaryBrush}" TextWrapping="Wrap" Margin="0,0,0,16" />

        <ui:Card Margin="0,0,0,8">
            <StackPanel>
                <TextBlock FontSize="16" FontWeight="Medium" Text="Enable Multi-Instance" Margin="0,0,0,4" />
                <TextBlock FontSize="12" Foreground="{DynamicResource TextFillColorTertiaryBrush}" Text="Allows running multiple Roblox clients simultaneously. Uses the multi-instance flag." Margin="0,0,0,8" />
                <ui:ToggleSwitch x:Name="MultiInstanceToggle" IsChecked="False" />
            </StackPanel>
        </ui:Card>

        <ui:Card Margin="0,0,0,8">
            <StackPanel>
                <TextBlock FontSize="16" FontWeight="Medium" Text="Max Instances" Margin="0,0,0,4" />
                <TextBlock FontSize="12" Foreground="{DynamicResource TextFillColorTertiaryBrush}" Text="Maximum number of concurrent Roblox instances" Margin="0,0,0,8" />
                <StackPanel Orientation="Horizontal">
                    <ui:Button Content="2" Tag="2" Click="MaxInstance_Click" Margin="0,0,8,0" Padding="12,4" />
                    <ui:Button Content="3" Tag="3" Click="MaxInstance_Click" Margin="0,0,8,0" Padding="12,4" />
                    <ui:Button Content="5" Tag="5" Click="MaxInstance_Click" Margin="0,0,8,0" Padding="12,4" />
                    <ui:Button Content="10" Tag="10" Click="MaxInstance_Click" Margin="0,0,8,0" Padding="12,4" />
                </StackPanel>
                <ui:TextBox x:Name="MaxInstancesInput" Width="100" PlaceholderText="Max" Margin="0,0,8,0" Text="3" />
            </StackPanel>
        </ui:Card>

        <ui:Button Content="Save" Icon="Save24" Margin="0,8,0,0" Click="Save_Click" />
        <TextBlock x:Name="MultiStatus" Margin="0,8,0,0" FontWeight="SemiBold" Visibility="Collapsed" />
    </StackPanel>
</ui:UiPage>
